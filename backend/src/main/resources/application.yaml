spring:
  config:
    import: optional:file:./.env[.properties], optional:file:./backend/.env[.properties], optional:file:./cinema-ticket/backend/.env[.properties], optional:file:./TTCS_N9_D23CN/cinema-ticket/backend/.env[.properties]

  application:
    name: backend

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cinema
    username: root
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost 
      port: 6379

  cache:
    type: redis

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${SPRING_MAIL_USERNAME}
    password: ${SPRING_MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:google-client-secret}
            scope:
              - openid
              - profile
              - email
  ai:
    openai:
      # --- CẤU HÌNH CŨ (Giữ lại để phòng khi lỗi có thể quay lại) ---
      # api-key: ${GROQ_API_KEY}
      # base-url: https://api.groq.com/openai
      # chat:
      #   options:
      #     model: llama-3.3-70b-versatile
      
      # --- CẤU HÌNH MỚI: GEMINI 2.5 PRO ---
      api-key: ${GEMINI_API_KEY} 
      base-url: https://generativelanguage.googleapis.com/v1beta/openai/ # URL tương thích OpenAI của Google (Nếu dùng OpenRouter thì đổi thành https://openrouter.ai/api/v1)
      chat:
        options:
          model: gemini-2.5-flash # Nếu dùng OpenRouter thì đổi thành google/gemini-2.5-pro gemini-2.0-flash
              
server:
  port: 8081
  servlet:
    context-path: /api

jwt:
  secret-key: ${JWT_SECRET_KEY}

app:
  seed:
    # On startup, ensure a small rolling schedule only (max 18 showtimes); existing slots are reused.
    on-startup: true
  demo-seed:
    # The large demo generator (past/future showtimes, sample bookings, and all missing tickets) stays off.
    on-startup: false
  oauth2:
    redirect-success-url: ${APP_OAUTH2_SUCCESS_URL:http://localhost:5173/oauth2/callback}
    redirect-failure-url: ${APP_OAUTH2_FAILURE_URL:http://localhost:5173/login}

vnpay:
  tmn-code: ${VNPAY_TMN_CODE}
  secret-key: ${VNPAY_SECRET_KEY}
  pay-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  api-url: https://sandbox.vnpayment.vn/merchant_webapi/api/transaction
  return-url: http://localhost:5173/payment/vnpay-return
